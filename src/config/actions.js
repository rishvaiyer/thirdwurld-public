// SPDX-License-Identifier: GPL-3.0-only

export const actionConfig = {
  allowedTypes: ['observe', 'move', 'interact', 'rest'],
  confidenceLevels: ['low', 'medium', 'high'],
  maxInputCharacters: 4_000,
  maxSummaryCharacters: 240,
  maxEvidenceIds: 12,
}

